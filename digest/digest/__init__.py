"""digest — генератор дайджеста недели из сырых заметок по канону.

Вход: сырые заметки (по строке на день / событие). Выход: дайджест —
заголовок со строчной, 3–5 пунктов ≤2 строк, только события, значимые для
читателя; плохие новости не прячутся; без эмодзи и антитез.

В отличие от canon-lens/judge (они ПРОВЕРЯЮТ), этот модуль ПОРОЖДАЕТ контент,
затем прогоняет его через тот же канон как self-check.
"""
from .core import make_digest

__all__ = ["make_digest"]
