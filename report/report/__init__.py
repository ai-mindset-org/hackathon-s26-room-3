"""report — единый отчёт из находок разных модулей по общему контракту.

Собирает `violations[]` от canon-lens (механика/redaction) и judge (смысл),
мёржит по `offset`, дедуплицирует, сортирует по месту в тексте и печатает один
отчёт: по пунктам канона, с пометкой источника (механика/судья/редакция).
Детерминированно, без LLM — надёжно на приёмке.
"""
from .merge import merge_reports, summary

__all__ = ["merge_reports", "summary"]
